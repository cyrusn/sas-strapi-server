'use strict';

/**
 * cohort-student-map router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::cohort-student-map.cohort-student-map');
